{% extends "FOSUserBundle::layout.html.twig" %}

{% block body %}
    <p>Registration Confirmed for {{ user.username }}</p>
    {% if targetUrl %}
    <p><a href="{{ targetUrl }}"> Back </a></p>
    {% endif %}
{% endblock body %}
